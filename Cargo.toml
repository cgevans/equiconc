[package]
name = "equiconc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nalgebra = "0.33"
pyo3 = { version = "0.25", optional = true, features = ["extension-module"] }

[features]
default = []
python = ["pyo3"]
