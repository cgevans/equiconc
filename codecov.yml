codecov:
  notify:
    wait_for_ci: true

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

parsers:
  cobertura:
    partials_as_hits: true
